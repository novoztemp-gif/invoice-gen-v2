/**
 * Centralized ERP Invoice & Batch Configuration Constants
 */

/** Maximum supported invoice generation capacity per batch (Purchase & Sales) */
export const MAX_INVOICES_PER_BATCH = 5000;

/** Default page size for paginated Supabase PostgREST queries */
export const SUPABASE_POSTGREST_PAGE_SIZE = 1000;
