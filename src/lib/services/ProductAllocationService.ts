import { InvoiceEngine } from "./InvoiceEngine";

export class ProductAllocationService {
  // Houses product selection, limits, and allocation algorithm for Sales/Purchase batches.
  static allocateProducts(selectedCustomers: string[], products: any[]) {
    // Custom allocation logic to be shared/extracted here in the future
    return [];
  }
}
